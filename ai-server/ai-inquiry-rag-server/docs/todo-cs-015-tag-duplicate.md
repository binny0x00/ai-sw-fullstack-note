# 중복 태그 문의

## 상황

사용자가 같은 태그가 여러 개 생긴 것 같다고 문의한다.

## 확인 포인트

- 대소문자나 공백 차이로 다른 태그처럼 보이는지 확인한다.
- 화면에서 중복 표시만 되는 것인지 DB에도 중복 생성되었는지 확인한다.

## 답변 가이드

태그 이름은 정규화되어 저장될 수 있으며, 비슷한 이름의 태그가 보이면 구체적인 예시를 요청한다.

## 개발팀 전달 기준

동일 태그가 중복 저장되면 데이터 정규화 버그로 분류한다.
