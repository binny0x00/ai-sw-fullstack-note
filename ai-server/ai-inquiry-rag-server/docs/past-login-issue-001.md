# 과거 문의: 로그인 버튼 클릭 시 반응 없음

## 문의 내용

사용자가 로그인 버튼을 클릭했지만 화면에 아무 반응이 없다고 문의했다.
브라우저 콘솔에는 CORS 정책 오류가 표시되었다.

## 처리 결과

백엔드 CORS 허용 origin에 프론트엔드 개발 서버 주소가 빠져 있었다.
허용 목록에 `http://localhost:5173`을 추가한 뒤 문제가 해결되었다.

## 재발 방지

개발, 스테이징, 운영 환경별 프론트엔드 origin을 환경변수로 관리한다.

